-- turboquant.gpr
project Turboquant is
   for Source_Dirs use (".");
   for Object_Dir use ".";
   for Exec_Dir use ".";
   for Main use ("tests.adb");
   
   package Compiler is
      for Default_Switches ("Ada") use ("-g", "-O2", "-gnata");
   end Compiler;
end Turboquant;
